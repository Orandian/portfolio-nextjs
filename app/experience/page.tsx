import Experience from "@/components/experience";
import Navigator from "@/components/navigator";

const page = () => {
  return (
    <div>
      <Experience />
      <Navigator />
    </div>
  );
};

export default page;
